@model string
@{
    ViewData["Title"] = "Create Instruction";
}

<h2>Create Instruction (Json)</h2>

<form asp-controller="Api" asp-action="CreateInstructionJson">
    <button type="submit" class="btn btn-default">Create Instruction (Json)</button>
    <div class="form-group">
        @Model
    </div>
</form>